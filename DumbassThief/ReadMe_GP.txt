// If you intend to make use of GP optimizations in your PRX project, then you
// need to use the following declaration on your entry point.
// With GP optimizations disabled (ie specifying -G0 for the compiler and linker), 
// the declaration should be ommited.
//
// int main() __attribute__((gp_safe));
